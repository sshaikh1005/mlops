apiVersion: v1

kind: Pod

metadata:
  name: iris-api

spec:

  containers:

    - name: iris-api

      image: iris-api:v1

      env:
        - name: MLFLOW_TRACKING_URI
          value: "http://mlflow-service:5000"

      ports:
        - containerPort: 8000